# File: frontend/.dockerignore
# Purpose: Specifies files and directories to ignore during Docker frontend image build.

node_modules
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Build output directory (will be created INSIDE the container during build)
dist
.vite

# Git files
.git
.gitignore
.gitattributes

# Local environment files if any for frontend specifically
.env.local
.env.*.local

# VS Code or other IDE settings within frontend dir
.vscode/
.idea/ # if frontend is a separate IDEA module